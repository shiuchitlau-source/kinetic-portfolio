# Prototype Instructions

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

The approved portfolio direction is the quieter “Silent Nameplate” polish: use a compact two-line intro, oversized ivory nameplate hero, sharp white-to-black transition, and an immediate two-project reveal with cinematic negative space. Preserve sparse corner navigation, restrained orange accents, real project content, hover previews, case-study drawers, and YouTube playback.

Use `/assets/b-logo-angular-option-2.png` consistently for the header monogram, browser favicon, and Apple touch icon.

The navigation closely follows ToyFight's current header anatomy and interaction while retaining Benjamin Lau's identity: a transparent fixed header, a compact dark monogram block that expands to the name on hover, and alternating square and pill section chips whose labels roll vertically and whose corners swap on hover. Do not restore the BL command terminal, slash keyboard shortcut, or “Press / for ?” prompt. On mobile, keep the expanded brand block, hide the central chips, and use the pill-shaped Menu trigger to open a full-viewport warm-grey menu inspired by ToyFight: identity block and black Close pill at the top, oversized centered navigation chips with Contact directly following Work, and restrained LinkedIn/contact metadata at the bottom using shiuchitlau@gmail.com. The overlay must also compress cleanly for short landscape mobile viewports.

The former Profile section is now a standalone `/services` page following the editorial structure of ToyFight's Services page: a large motion-focused thesis and positioning statement followed by numbered service chapters with a narrow portrait project visual on the left and the title, capability tags, concise explanation, and links to relevant case studies in the wider right field. The four offers are Motion Direction, Brand Motion Systems, 3D & CGI, and Campaign Content. Keep Services off the main page. Main-page navigation links to `/services`; the Services-page navigation marks Services active and links Home and Work back to the main page. Do not restore the old Profile section or an in-page `#services` target.

The `/services` page also follows ToyFight's restrained reveal language: introduce the kicker, then lift each thesis line through its own clipping mask with a short stagger, followed by the two positioning paragraphs. Reveal each service chapter only as it enters the viewport, using an upward image mask, a separately masked title, and a quiet cascade through tags, description, and selected-work links. Keep image parallax subtle and vertical. Do not apply these reveals to the main portfolio page, and present the fully composed layout without motion when reduced motion is requested.

Close the Services content with a ToyFight-style selected-clients chapter: lead with the large “Clients” heading in the left field and place the concise positioning statement directly beneath it, paired with two equal, top-aligned columns of compact client labels in the wider right field. Use content-width pale stone labels with no border and subtle corners, scaled to 26px high with 8px horizontal padding. Set the client names in the same tracked DM Sans utility voice as the footer's “Available for selected projects” line. Align every label to the right edge of its column. On hover, roll each name upward through a clipped one-line mask while its duplicate enters from below using the reference's slow ease-out timing; disable that label roll for reduced motion. Keep it editorial rather than a logo wall or carousel, and reveal the heading, statement, and names with the same restrained viewport-triggered cadence used by the service chapters.

On desktop and tablet, vertically center the complete two-column client-label group against the combined “Clients” heading and body-copy block. On mobile, stack the label columns beneath the paragraph.

The current title-section direction refines that nameplate into an editorial masthead: use a compact monogram/navigation/availability rail, a layered orange halftone horse herd with cobalt-blue shadow pixels, and a continuous “MAKE PIXELS MOVE” marquee above the work transition. Keep the darkest horse shade blue (`#1746d1`) while preserving the remaining orange palette. Render every horse as code-driven canvas pixels from the normalized coordinate data in `src/horseFrames.js`; do not restore GIF, video, or raster sprite playback. Give every horse a fully opaque paper-coloured silhouette beneath its coloured dots. Preserve visual depth through horse size and dot intensity: the lighter small horses must remain behind the marquee, while the stronger foreground horses sit above it and prevent letters showing through their silhouettes. Keep the marquee as a true `100vw` full-bleed layer at every responsive breakpoint, independent of the padded hero content width. Each continuous marquee loop must explicitly begin with “MAKE” aligned to the left edge and move without gaps. Keep every horse anchored to a fixed location with a slow, subtle independent wiggle like the original single-horse drift; do not make the horses travel across the screen. Vary their sizes, but keep the entire herd smaller than the former dominant single horse. The herd itself is the only motion control: a click anywhere on the scene pauses the frame cycle, every wiggle, and the marquee; the next click resumes all motion with equivalent keyboard behavior. Do not restore a separate motion switch. Reduced-motion mode shows a stable layered composition. Do not restore “Designing Bold Motion Experiences,” “MOTION,” another static display headline, or the removed “Motion for” discipline rail in this title slot. On mobile, keep the herd visible without clipping, reserve a clear text-only band above every horse frame for the “Independent motion designer” kicker, and replace the desktop links with the fullscreen Menu trigger.

On desktop landscape viewports, the entire hero should occupy the first screen at `100svh`: keep the floating navigation near the top, scale the horse from both available width and height so the full silhouette remains visible, and hold the discipline rail at the bottom of the viewport. Do not force that landscape sizing onto tablet or mobile portrait layouts.

The header identity uses the selected orange angular “B” monogram in `public/assets/b-logo-angular-option-2.png`, paired with the exact name “Benjamin Lau” as a compact two-line black/grey lockup. Preserve its full-height stem, rounded bowls, and rising diagonal bridge. Do not restore the L or forward slash, and do not replace the mark with a typed initial.

Use Apple-style interaction polish as a craft layer rather than a new visual theme: preserve the editorial layout and sharp media frames, keep glass only for floating functional chrome, provide immediate press feedback and at least 44px touch targets, make overlays enter and leave along the same path, and support reduced motion, reduced transparency, and increased contrast. Do not convert the page into rounded card grids or a generic Apple product-page imitation.

Use the orange pixel-grid transition from `pixel-transition.webflow.io` as the site-wide navigation language. On first load, reveal the hero through a randomized 12-by-8 field of crisp orange tiles. For internal section links, let the same tiles fully cover the current view before changing the hash/scroll position, then remove them in a second randomized step sequence. Generate a fresh timing pattern for every run, blending scattered bursts with a restrained directional bias; cover and reveal must use different orders so the effect never reads as a repeated wipe. Keep the transition short, block pointer input only while it is active, and bypass it entirely when reduced motion is requested.

For main-page section navigation, the pixel grid is the only transition. Once the tiles fully cover the viewport, jump instantly to Work, Contact, the project index, or the top before revealing the destination. Do not combine the pixel transition with smooth scrolling.

The hero focal object is now the original orange pixel horse animation over the full-bleed Motion Reel 2025 background video, not the Rodin 3D model. Treat the reel with the edge-to-edge scale and restrained interface framing of the Interbrand reference while retaining this portfolio’s own orange pixel identity. Autoplay the muted looping reel on landing, pause it by default for reduced-motion users, and keep a visible 44px play/pause control. Keep one horse centered in the hero using the approved canvas frame data and orange palette. Render the horse 20% smaller than its former desktop size, using a persistent 528px cap and 38.4vw fluid width across desktop layouts so the reduction remains visible even when browser chrome shifts a nominal 16:9 viewport; preserve the existing 290px mobile sizing. As the user scrolls through the title page, scale the horse continuously from 100% at the top to 30% when the hero fully exits, using one requestAnimationFrame-batched transform update and restoring 100% when reduced motion is requested. Compose that scale after the centered translate with an explicit 50%/50% transform origin, so the horse shrinks symmetrically around its visual centre rather than pinning either side. Keep the visible “MOVING / PIXELS” title lockup hidden while preserving the semantic `h1` as live screen-reader text. The horse runs automatically, pauses and resumes by click or keyboard, shows a stable frame for reduced motion, and keeps a paper-coloured silhouette beneath its dots.

Project titles in the black work section and the opened case-study heading remain in DM Sans at 600 weight. Preserve their existing responsive sizes, line heights, wrapping, and nearby serif metadata.

Within the black work section, use large editorial project frames rather than a compact constellation. Wise should dominate the upper right, Samsung should form a substantial second beat on the left, and later projects should continue in a widely spaced staggered sequence. Keep scroll-driven scale motion on the frames.

Use Wise Future Store and Samsung Skate Park as the two canonical project-title patterns. On desktop, right-aligned imagery uses the Wise pattern with its label on the left, while left-aligned imagery uses the Samsung pattern with its label on the right. Keep each label optically close to its image rather than floating at the outer page edge. The visible title-to-image gap must be identical across every project even while scroll scaling is active; anchor scaling to the title-facing edge of each image. Keep the project number, title, category, and year typography identical across every item. At tablet and mobile widths, every title stacks closely below its image and aligns to the image’s leading edge, with scaling anchored to the image’s bottom edge. Project frames and their labels must never overlap at any responsive width, including while scroll-driven scaling is active. Preserve the staggered look with normal flow and reserved vertical gaps rather than fixed absolute top positions.

Project subpages should use a compact introduction and metadata rail followed by a dominant full-width lead image. Arrange the first two supporting images in an asymmetric 42/58 split, let later media return to full width, and keep previous/next navigation balanced at the bottom. When a project has a YouTube film, the privacy-enhanced player occupies the same lead-media slot. Collapse the gallery to a single column on mobile.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
